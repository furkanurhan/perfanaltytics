{
  "name": "perfanaltytics",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "node server.js",
    "build": "npm run build:reporter && npm run build:dashboard",
    "build:reporter": "cd reporter && npm install && npm run build && cd ..",
    "build:dashboard": "cd dashboard && npm install && npm run build && cd ..",
    "install:dashboard": "cd dashboard && npm install && cd ..",
    "install:reporter": "cd reporter && npm install && cd ..",
    "install:all": "npm install && npm run install:dashboard && npm run install:reporter"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/furkanurhan/perfanaltytics.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/furkanurhan/perfanaltytics/issues"
  },
  "homepage": "https://github.com/furkanurhan/perfanaltytics#readme",
  "dependencies": {
    "body-parser": "^1.19.1",
    "compression": "^1.7.4",
    "cors": "^2.8.5",
    "dotenv": "^14.2.0",
    "express": "^4.17.2",
    "mongoose": "^6.1.7",
    "morgan": "^1.10.0",
    "serve-static": "^1.14.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.15"
  }
}
